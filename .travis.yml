language: python
cache: pip

env:
  - TOX_ENV=py33
  - TOX_ENV=py34
  - TOX_ENV=py35

install:
  - pip install tox

script:
  - tox -e $TOX_ENV

after_success:
  - bash <(curl -s https://codecov.io/bash)