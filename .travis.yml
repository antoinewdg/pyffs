language: python
cache: pip

python:
  - "3.5"

env:
  - TOX_ENV=py35
  - TOX_ENV=py34

install:
  - pip install tox
#  - pip install -r requirements-dev.txt
#  - pip install python-coveralls

script:
  - tox

after_success:
  - bash <(curl -s https://codecov.io/bash)